
serve.verbose.chrome [

    GET "/" [
        
    ]
    
    GET "/(?<pth>.+)" $[pth][
        ; print ["asked for:" pth]
        ; print ["->" ./"3rdparty/monaco/" ++ pth]
        ; req: pth
        ; if contains? pth "#" ->
        ;     req: first split.by:"#" pth
        ; src: read ./ req

        ; (suffix? req "js")? [ print "SENDING JS" send.js src]
        ;                     -> send src
    ]
]